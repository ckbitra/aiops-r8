Where	What to check
Step Functions	AWS Console → Step Functions → State machines → aiops-r8-patch-workflow → Executions
EventBridge	AWS Console → EventBridge → Rules → aiops-r8-patch-schedule → Metrics (invocations)
CloudWatch Logs	AWS Console → CloudWatch → Log groups → /aws/lambda/aiops-r8-*
CloudWatch Metrics	EventBridge rule metrics (Invocations, TriggeredRules)



1) Create terraform code using modularity to create:
add 2 rhel8 free tier servers 
add 2 free tier windows servers
Use tagging for all the servers.


2)  Use the required amazon services like:
lambda, eventbridge, step functions, aws systems manager
Create terraform code (modularity) for a production-safe patch workflow
using bedrock LLM: us.amazon.nova-2-lite-v1:0
Add comments to each section of the terraform code.

3) Scan the ec2 instances and windows instances and provide a scan report.
add rhel8 report to docs/rhel8_report.md and provide the location where it is being stored on the server.
add windows report to docs/windows_report.md and provide the location where it is being stored on the server.

4)
The patching should address only CVE

5)
provide a docs/pre_patch.md with details of SSM Document that has the patching information.

6)
provide SSM Document and save it to docs/post_patching.md 


Readme files:
add diagram to docs/diagram.md
add terraform code workflow to docs/terraform_workflow.md
add complete project workflow to docs/complete_project.md
add explanation about each component being used to docs/components.md
add Document details being used by aws SSM in docs/ssm_documents.md
